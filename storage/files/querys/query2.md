## 파트너사 별 가입자

```sql
SELECT TO_CHAR(REG_DATE, 'YYYYMMDD'), PARTNERSHIP, COUNT(1)
FROM MB_MST
WHERE REG_DATE >= SYSDATE - #param1
GROUP BY TO_CHAR(REG_DATE, 'YYYYMMDD'), PARTNERSHIP
ORDER BY 1 DESC;
```
